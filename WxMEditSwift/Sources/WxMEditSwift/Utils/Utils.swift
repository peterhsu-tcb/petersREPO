import Foundation

// Reserved for future shared helpers (NSRange <-> String.Index conversion,
// keyboard-shortcut DSL, etc.). Kept as a placeholder so the Utils target
// directory is non-empty in source control.
enum Utils {}
